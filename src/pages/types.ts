export interface BookData {
    id: number;
    title: string;
    subTitle: string;
    description: string;
    author: string;
    publisher: string;
    coverImgUrl: string;
}

// interface는 객체타입을 정의하기 위해 사용함